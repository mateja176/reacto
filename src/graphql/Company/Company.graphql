type Company implements NamedNode {
  id: ID!
  name: String!
  owner: User!
  users: [User!]!
  questionnaires: [Questionnaire!]!
  questionnaireConfigurations: [QuestionnaireConfiguration!]!
}

type Query {
  company(id: ID!): Company!
}
