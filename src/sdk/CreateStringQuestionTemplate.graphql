mutation CreateStringQuestionTemplate(
  $input: CreateStringQuestionTemplateInput!
) {
  createStringQuestionTemplate(input: $input) {
    id
    name
    label
    optional
    rule
    default
  }
}
